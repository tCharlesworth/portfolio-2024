---
import StoryblokComponent from "@storyblok/astro/StoryblokComponent.astro"
import Panel from "../../internal/panel.astro"

const { blok } = Astro.props
---

<Panel blok={blok}>
  {
    blok.panels?.map((subBlok) => {
      return <StoryblokComponent blok={subBlok} />
    })
  }
</Panel>